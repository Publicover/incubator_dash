require 'test_helper'

class RemindersControllerTest < ActionDispatch::IntegrationTest

  test "the truth" do
    the_truth = true
    assert the_truth, true
  end

  # test "say_hi" do
  #   assert_difference 'ActionMailer::Base.deliveries.size', +1 do
  #     post send_email_path, params: {email: "to@example.com"}
  #   end
  #
  # end

end
